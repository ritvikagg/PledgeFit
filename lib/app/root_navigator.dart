import 'package:flutter/material.dart';

/// Used so `/daily` and `/result` cover the bottom shell (full-screen routes).
final GlobalKey<NavigatorState> rootNavigatorKey = GlobalKey<NavigatorState>();
